---
name: promote-prod
description: "Muestra el estado de las compuertas para producción. No despliega: la promoción la hace una persona con kit.js prod."
agent: director
---
Ejecuta el comando `promote-prod` del kit siguiendo la skill `promote-prod` (en `.github/skills/` del proyecto o en `~/.copilot/skills/`). Nunca ejecutes `node kit.js prod`.
